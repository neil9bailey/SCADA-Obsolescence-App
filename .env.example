# Quick local use defaults to SQLite when DATABASE_URL is omitted.
APP_NAME=SCADA Lifecycle Command Centre
SEED_DEMO=true
AUTO_CREATE_SCHEMA=true
DATA_DIR=./data

# Managed PostgreSQL example:
# DATABASE_URL=postgresql+psycopg://scada_app:replace-me@db.example.internal:5432/scada_lifecycle
